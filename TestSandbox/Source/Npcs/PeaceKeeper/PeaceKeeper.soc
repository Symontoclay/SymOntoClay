app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        @@host.SomeVeryLongSilentFun()[: timeout = 100, on weak canceled { 'on weak canceled' >> @>log; } :];
        'End' >> @>log;
    }
}
