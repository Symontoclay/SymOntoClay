app PeaceKeeper
{
    fun a() => 
    {       
        wait 1;
        '`a` has been called!' >> @>log;
    }

    on Enter =>
    {
        'Begin' >> @>log;
        a()[: timeout = 0.1, on weak cancel { 'on weak cancel' >> @>log; } :];
        'End' >> @>log;
    }
}
