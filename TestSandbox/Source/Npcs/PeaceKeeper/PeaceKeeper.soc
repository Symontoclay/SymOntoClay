app PeaceKeeper is [0.5] exampleClass
{
    {: barrel(#a) :}
	{: see(I, #a) :}

    on Init => {
	     'Begin' >> @>log;

		 //use @@self is linux;

		 select {: see(I, barrel) :} >> @>log;
		 //select {: son(?x, ?y) :} >> @>log;

		 //insert {: >: { bird (#1234) } :};

		 //exampleClass is not human >> @>log;

		 //@@host.`go`(to: #@[10]);

		 "End" >> @>log;
	}

	//on {: see(I, barrel) :} => {
	//     "L" >> @>log;
	//}
}
