app PeaceKeeper
{
private:
	{: male(#Tom) :}
	{: parent(#Piter, #Tom) :}
	{: {son($x, $y)} -> { male($x) & parent($y, $x)} :}

	on Init => {
	    'Begin' >> @>log;
	    select {: son(@a, $y) :} >> @>log;
		'End' >> @>log;
	}

private:
	@a = #Tom;
}
