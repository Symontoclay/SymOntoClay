app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        'End' >> @>log;
    }

    var @a = 15;

	on {: see(I, #a) :} as `trigger 1` alias `Alarm trigger` => 
    {
	    'S' >> @>log;
	}

    on (`Alarm trigger` & @a is 15)
    {
        'D' >> @>log;
    }
}
