app PeaceKeeper
{
    fun a(): boolean => 
    {
        '`a` has been called!' >> @>log;
        return {: enemy($_) & see(I, $_) & alive($_, true) :};
    }

    on Enter =>
    {
        'Begin' >> @>log;
        a() >> @>log;
        'End' >> @>log;
    }
}
