app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        var @task = @@host.SomeVeryLongSilentFun~() [: timeout = 100 :];
        on @task weak canceled { 'on weak canceled' >> @>log; };
        wait 1000;
        'End' >> @>log;
    }
}
