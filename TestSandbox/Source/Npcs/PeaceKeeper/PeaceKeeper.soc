app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        SomeAutoProp = {: enemy($_) & see(I, $_) & alive($_, true) :};
        SomeAutoProp >> @>log;
        'End' >> @>log;
    }

    prop SomeAutoProp: boolean;
}
