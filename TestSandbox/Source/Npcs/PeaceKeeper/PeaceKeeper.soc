app PeaceKeeper is [0.5] exampleClass
{
    on Init => {
	     'Begin' >> @>log;

		 //use @@self is linux;

		 select {: son(?x, ?y) :} >> @>log;

		 //insert {: >: { bird (#1234) } :};

		 //exampleClass is not human >> @>log;

		 //@@host.`go`(to: #@[10]);

		 "End" >> @>log;
	}

	on {: see(I, #`Gun 1`) :} => {
	     "L" >> @>log;
	}
}
