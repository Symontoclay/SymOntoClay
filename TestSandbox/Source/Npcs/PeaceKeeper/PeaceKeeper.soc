app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        @@host.SomeVeryLongSilentFun~~()[: timeout = 100, cancel, on weak canceled { 'on weak canceled' >> @>log; } :];
        'End' >> @>log;
    }
}
