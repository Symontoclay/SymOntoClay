app PeaceKeeper
{
    //{: see(I, #a) :}

    on Init
    {
        'Begin' >> @>log;

        //@@host.take(#@(gun));        
        //@@host.take(#@(barrel));
        //@@host.`rotate`(30);
        //@@host.`rotate`(NULL);
        //@@host.`rotate`(#@(gun));
        //@@host.take(NULL);
        @@host.fire();

        'End' >> @>log;
    }

    on {: see(I, #a) :}
    {
        'Begin see(I, #a)' >> @>log;
        @@host.aim();
        'End see(I, #a)' >> @>log;
    }
}
