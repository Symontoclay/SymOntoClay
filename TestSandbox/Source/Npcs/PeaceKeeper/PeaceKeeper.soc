app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        @@host.SomeVeryLongSilentFun~~()[: timeout = 100, mode = weak cancel, on weak canceled { 'on weak canceled' >> @>log; } :];
        'End' >> @>log;
    }
}
