app PeaceKeeper
{
    on Enter =>
    {
        'Begin' >> @>log;
        var @b: boolean = true;
        @b >> @>log;
        @b = {: enemy($_) & see(I, $_) & alive($_, true) :};
        @b >> @>log;
        'End' >> @>log;
    }
}
