app PeaceKeeper
{
    {: gun(M16) :}

	on add fact ($_ >> @x)
	{
	    'on add fact ($_ >> @x)' >> @>log;
	    @x >> @>log;
		reject;
	}

	on {: hear(I, $x) & gun($x) & distance(I, $x, $y) :} ($x >> @x, $y >> @y)
    {
        @x >> @>log;
        @y >> @>log;
        '!!!M16!!!!' >> @>log;
    }
}
